namespace MControl.GridView
{
    using System;

    /// <summary>Defines how a <see cref="T:MControl.GridView.Grid"></see> column can be sorted by the user.</summary>
    /// <filterpriority>2</filterpriority>
    public enum GridColumnSortMode
    {
        NotSortable,
        Automatic,
        Programmatic
    }
}

