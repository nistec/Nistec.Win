namespace MControl.GridView
{
    using System;

    /// <summary>Defines constants that indicate whether content is copied from a <see cref="T:MControl.GridView.Grid"></see> control to the Clipboard.</summary>
    /// <filterpriority>2</filterpriority>
    public enum GridClipboardCopyMode
    {
        Disable,
        EnableWithAutoHeaderText,
        EnableWithoutHeaderText,
        EnableAlwaysIncludeHeaderText
    }
}

